import { Button } from "@repo/shared";
import { Icon } from "@repo/shared";

const Home = () => {
  return (
    <>
      <Button />
      <p className="text-primary">dddd</p>
      <Icon name="Back" />
    </>
  );
};

export default Home;
